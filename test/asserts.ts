import * as asserts from "https://deno.land/std@0.69.0/testing/asserts.ts";
export const assert = asserts.assert;
export const assertEquals = asserts.assertEquals;
export const assertThrows = asserts.assertThrows;
